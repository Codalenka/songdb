// Remote true points to the create.js.erb file.
var html = "<li><%= @song.name %><br><%= escape_javascript link_to 'delete', song_path(@song), method: 'delete', remote: true %></li>";

// Makes sure field is empty after submitting.
$('#song_input').val('');

// Tells the new song where to appear on the page.
$('ul').append(html);